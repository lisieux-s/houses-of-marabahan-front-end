import { Modal } from '../../../../components/Modal/style';

export default function EditItem({modalIsOpen, id, name, description, sprite}) {
    return(
        <Modal modalIsOpen={modalIsOpen}>
            edit item
        </Modal>
    )
}